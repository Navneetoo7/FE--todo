export enum TaskPriority {
    LOW = 'low',
    MEDIUM = 'medium',
    HIGH = 'high',
  }
  
  export enum TaskStatus {
    COMPLETED = 'completed',
    PENDING = 'pending',
    UNDER_PROCESS = 'under_process',
  }

  export enum Gender {
    MALE = "male",
    FEMALE = "female", 
  }